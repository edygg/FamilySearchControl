<div class="jumbotron">
  <h1>Agregar una ordenanza</h1>
  <p>Estas agregando una ordenanza que fue realizada en nombre de <%= "#{@ancestor.names} #{@ancestor.last_names}." %></p>
</div>

<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= render 'form' %>
  </div>
</div>
<div class="btn-group">
  <%= link_to sender_ancestors_path(@sender), class: 'btn btn-default' do %>
  <%= icon('arrow-left') %>
  <% end %>
</div>
