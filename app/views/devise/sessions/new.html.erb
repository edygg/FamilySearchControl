<div class="jumbotron">
  <h1>Entrar</h1>
  <p>Utiliza el correo electrónico y contraseña con que te has registrado para acceder.</p>
</div>

<div class="col-sm-6 col-sm-offset-3">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-horizontal' }) do |f| %>
     <div class="form-group">
      <%= f.label :email, 'Correo electrónico', class: 'col-sm-3 control-label' %>
      <div class="col-sm-9">
        <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'example@company.me' %>
      </div>
    </div>
  
    <div class="form-group">
      <%= f.label :password, 'Contraseña', class: 'col-sm-3 control-label' %>
      <div class="col-sm-9">
        <%= f.password_field :password, autocomplete: 'off', class: 'form-control' %>
      </div>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9">
          <div class="checkbox">
            <%= f.check_box :remember_me %> <%= f.label :remember_me, 'Recuérdame' %>
          </div>
        </div>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.submit "Entrar", class: 'btn btn-success' %>
    </div>
  <% end %>
</div>