<div class="jumbotron">
  <h1>Regístrate</h1>
  <p>Podrás tener acceso a todas las características de adminsitración que FamilySearchControl ofrece solo debes proveer tu correo y una contraseña.</p>
</div>

<div class="col-sm-6 col-sm-offset-3">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal' }) do |f| %>
    <%= devise_error_messages! %>
    
    <div class="form-group">
      <%= f.label :email, 'Correo electrónico', class: 'col-sm-3 control-label' %>
      <div class="col-sm-9">
        <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'example@company.me' %>
      </div>
    </div>
    
    <div class="form-group">
      <%= f.label :password, 'Contraseña', class: 'col-sm-3 control-label' %>
      <div class="col-sm-9">
        <%= f.password_field :password, autocomplete: 'off', class: 'form-control' %>
      </div>
    </div>
    
    <div class="form-group">
      <%= f.label :password_confirmation, 'Confirmar contraseña', class: 'col-sm-3 control-label' %>
      <div class="col-sm-9">
        <%= f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.submit "Registrarse", class: 'btn btn-default' %>
    </div>
  <% end %>
</div>
